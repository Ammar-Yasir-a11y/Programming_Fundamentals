#include<iostream>
using namespace std;
int main(){

   //   TASK 05
   
   int *ptr1=NULL;
   if (ptr1==NULL)
   {
   	
   	cout<<"It is Null Pointer because it has no valid memory address."<<endl;
   	
    }else{
   cout<<"The pointer holds the memory address:"<<ptr1<<endl;
         }
    return 0;
   }
